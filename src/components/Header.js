import Hamburger from './Hamburger';
import NavMain from './NavMain';

function Header() {
    return(    
        <header>
            <NavMain />
            <Hamburger />
        </header>
    )
}

export default Header;